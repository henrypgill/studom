{
  "name": "@studom/database",
  "version": "0.0.0",
    "main": "./index.ts",
    "types": "./index.ts",
  "scripts": {
    "dev": "bunx prisma studio --browser=none",
    "migrate": "bunx prisma migrate dev && bunx prisma generate",
    "db:generate": "bunx prisma generate"
  },
  "devDependencies": {
    "prisma": "5.5.2"
  },
  "dependencies": {
    "@prisma/client": "5.5.2"
  }
}